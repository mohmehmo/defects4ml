[DEFAULT]
python = 3.7
source = stackoverflow
framework = keras
url = https://stackoverflow.com/questions/56103207
property = Correctness
bug_type = Missing preprocessing
root_cause = Missing preprocessing
symptom = Bad performance
description = Because of skipping necessary preprocessing step (data shuffling), accuracy of the model stays at near 0.

[RUN]
entry_point = pytest
