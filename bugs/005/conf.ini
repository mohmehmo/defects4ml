[DEFAULT]
python = 3.6
source = github
framework = tensorflow
url = https://github.com/gabrieleangeletti/Deep-Learning-TensorFlow/commit/1bcc1ef
property = Correctness
bug_type = Missing API call
root_cause = Missing API call
symptom = Bad performance
description = Because of problematic calculations in the manually implemented softmax layer, accuracy of the model stays near 10 %%. Thus, they addressed this issue by porting the output of softmax function into built-in softmax layer type. 

[RUN]
entry_point = pytest
